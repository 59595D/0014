# 收纳管家 - 国内构建指南

## 🇨🇳 国内环境优化说明

项目已配置国内镜像源,解决以下问题:
- ✅ 依赖下载慢
- ✅ Google服务访问受限
- ✅ 语音识别依赖Google服务
- ✅ 网络连接问题

---

## 📦 已配置的国内镜像

### 1. 阿里云镜像 (主要)
- Google仓库
- 公共仓库
- Gradle插件
- 中央仓库

### 2. 腾讯云镜像 (备用)
- Maven公共仓库

### 3. 华为云镜像 (备用)
- Maven仓库

### 4. JitPack
- GitHub项目依赖

---

## 🚀 快速构建方案

### 方案A: 使用Android Studio + 国内镜像(推荐)

#### 步骤:

1. **下载Android Studio (国内镜像)**
   - 阿里云镜像: https://developer.android.google.cn/studio
   - 或者直接下载: `https://dl.google.com/dl/android/studio/install/2023.1.1.28/android-studio-2023.1.1.28-windows.exe`

2. **配置Android Studio使用国内镜像**

   打开Android Studio后,创建 `init.gradle` 文件:

   位置: `C:\Users\你的用户名\.gradle\init.gradle`

   内容:
   ```gradle
   allprojects {
       repositories {
           maven { url 'https://maven.aliyun.com/repository/google' }
           maven { url 'https://maven.aliyun.com/repository/public' }
           maven { url 'https://maven.aliyun.com/repository/gradle-plugin' }
           maven { url 'https://maven.aliyun.com/repository/central' }
           google()
           mavenCentral()
       }
   }

   buildscript {
       repositories {
           maven { url 'https://maven.aliyun.com/repository/google' }
           maven { url 'https://maven.aliyun.com/repository/public' }
           maven { url 'https://maven.aliyun.com/repository/gradle-plugin' }
           google()
           mavenCentral()
       }
   }
   ```

3. **打开项目**
   - File → Open → 选择项目文件夹
   - 等待Gradle同步(使用国内镜像会快很多)

4. **构建APK**
   - Build → Build Bundle(s) / APK(s) → Build APK(s)

### 方案B: 使用在线构建平台

#### GitHub Actions (免费)
1. 将代码上传到GitHub
2. 创建 `.github/workflows/build.yml`:
   ```yaml
   name: Build APK

   on: [push]

   jobs:
     build:
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v3
         - name: Set up JDK 11
           uses: actions/setup-java@v3
           with:
             java-version: '11'
             distribution: 'temurin'
         - name: Grant execute permission for gradlew
           run: chmod +x gradlew
         - name: Build with Gradle
           run: ./gradlew assembleDebug
         - name: Upload APK
           uses: actions/upload-artifact@v3
           with:
             name: app-debug
             path: app/build/outputs/apk/debug/*.apk
   ```
3. 提交后自动构建,可下载APK

#### Codemagic (免费额度)
1. 访问: https://codemagic.io
2. 关联GitHub仓库
3. 配置构建流程
4. 获取APK

---

## ⚙️ 功能适配说明

### 1. 语音识别功能
**问题**: 依赖Google语音识别服务

**解决方案**:
- 如果设备有Google服务: 可以正常使用
- 如果没有Google服务: 语音输入功能不可用,但不影响其他功能
- 可以集成百度/科大讯飞语音识别(需要API key)

**修改建议**:
```kotlin
// 在 SpeechRecognitionHelper.kt 中
// 可以替换为百度语音识别
// 或者直接禁用语音输入功能
```

### 2. 图片选择
**状态**: ✅ 完全可用
- 使用系统相册,不依赖网络
- 图片保存在应用私有目录

### 3. 数据存储
**状态**: ✅ 完全可用
- 使用Room本地数据库
- 无需网络连接

### 4. 所有核心功能
- ✅ 物品添加/编辑/删除
- ✅ 搜索和浏览
- ✅ 折叠屏适配
- ✅ 保质期提醒
- ⚠️ 语音识别(需要Google服务)

---

## 📋 常见问题

### Q1: Gradle同步失败
**A**: 确保配置了`init.gradle`文件,使用阿里云镜像

### Q2: 下载依赖很慢
**A**:
1. 检查是否使用了国内镜像
2. 确认网络连接
3. 使用代理加速

### Q3: 语音识别不工作
**A**: 
- 国内Android设备通常没有Google服务
- 可以禁用语音输入,不影响其他功能
- 或集成百度/科大讯飞语音SDK

### Q4: APK文件太大
**A**:
- Release版本会更小
- 启用代码混淆和资源压缩
- 使用 `app/build/outputs/apk/release/` 的版本

---

## 🔧 进一步优化建议

### 1. 替换语音识别
```kotlin
// 使用百度语音识别
// 需要申请API key: https://ai.baidu.com/tech/speech
implementation("com.baidu.aip:java-sdk:4.16.13")
```

### 2. 使用国内崩溃统计
```kotlin
// 腾讯Bugly
implementation("com.tencent.bugly:crashreport:3.4.3")
```

### 3. 添加应用商店分发
- 华为应用市场
- 小米应用商店
- 应用宝
- OPPO应用市场
- vivo应用商店

---

## 📱 APK下载位置

构建成功后:
```
调试版: app/build/outputs/apk/debug/app-debug.apk
发布版: app/build/outputs/apk/release/app-release.apk
```

---

## 💡 最终建议

**最快获得APK的方式**:
1. 使用Android Studio打开项目
2. 已配置国内镜像,同步会很快
3. 一键构建APK

**如果完全没有Android开发环境**:
1. 使用GitHub Actions自动构建(免费)
2. 或使用Codemagic等在线构建平台

**项目已针对国内环境优化**,主要功能都可以正常使用!
